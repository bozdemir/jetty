# JeTTY v0.7.0

Detachable tabs and a lot more themes.

## Highlights

- **Detach a tab into its own window** — press **`Ctrl+Shift+D`** to pop the
  active tab out into its own terminal window; press it again (or close the
  window) to reattach. Extra windows are damage-driven, so they don't cost idle
  CPU, and there's no desktop-environment-specific code — just winit.
- **22 built-in themes** (up from 5) — Catppuccin (Mocha/Macchiato), Tokyo Night,
  Gruvbox, Dracula, Onyx, Nord, Solarized (dark/light), One Dark, Monokai (+Pro),
  Everforest, Rosé Pine, Kanagawa, Material, Ayu (dark/mirage), Tomorrow Night,
  Oceanic Next, GitHub Dark, Palenight. The theme picker is now a scrollable
  dropdown with a live color-swatch preview per theme.

## Install

```bash
curl -fsSL https://raw.githubusercontent.com/bozdemir/JeTTY/main/install.sh | sh
```

Or a `.deb` / **AppImage** from the assets below (the installer verifies
`SHA256SUMS.txt`). Build from source: `cargo build --release`.

## Artifacts

| File | What |
|---|---|
| `jetty-0.7.0-x86_64-linux.tar.gz` | Portable Linux binary + assets |
| `jetty_0.7.0_amd64.deb` | Debian / Ubuntu package |
| `JeTTY-0.7.0-x86_64.AppImage` | Universal Linux AppImage |
| `SHA256SUMS.txt` | Checksums |

**Full changelog:** [`CHANGELOG.md`](https://github.com/bozdemir/JeTTY/blob/main/CHANGELOG.md).
